# PR Reviewer — Goals

> Current objectives. Clear session goals after each review; standing goals persist.

---

## Standing Goals

- After every review: append to `log.md`, update `memory.md` if new knowledge was gained
- Never approve a PR where `.playwright-mcp/` snapshot files remain tracked in the branch
- Never approve a PR where any test file is missing `[TestCaseId]` prefixes

---

## Current Session

_No active review. Paste a PR URL or say `Review my current branch against main` to begin._

# Nat — Goals

> Current priorities and next steps.
> Completed items are removed; standing items persist.

---

## Standing Goals

- Keep `memory.md` current — update after every large task or when prompted
- Keep agent files in sync with any new repo conventions discovered

---

## Current

**Skeleton completion** — make this repo immediately forkable for new QA projects.

- [x] Matt Pocock skills committed + `.claude/commands/` wired for slash commands
- [x] Nat agent merged with project context (ai-helper removed)
- [ ] `playwright.config.ts` — verify/complete config
- [ ] `package.json` scripts (`test:api`, `test:e2e`)
- [ ] Example API test
- [ ] Example E2E test

---

## Backlog

- CI pipeline review (GitHub Actions workflows)
- Auth helper patterns
- Reporter config
